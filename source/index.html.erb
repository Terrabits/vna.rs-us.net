---
layout: default
title:  Welcome
---

<p class="lead">
  Welcome to the Rohde &amp; Schwarz North America vector network analyzer
  software page. This page offers VNA software applications and
  software development tools.
</p>

<%= partial 'partials/section', locals: { section: data.sections[:applications] } %>
<%= partial 'partials/section', locals: { section: data.sections[:python      ] } %>
<%= partial 'partials/section', locals: { section: data.sections[:rsatoolbox  ] } %>
<%= partial 'partials/section', locals: { section: data.sections[:blog        ] } %>
<%= partial 'partials/section', locals: { section: data.sections[:contact     ] } %>
